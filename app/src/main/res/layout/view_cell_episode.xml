<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>


        <variable
            name="clickHandler"
            type="au.com.carsales.emovie.utils.base.databinding.BaseBindClickHandler" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/primaryLightColor"
        android:foreground="?android:attr/selectableItemBackground"
        android:padding="8dp">


        <ImageView
            android:id="@+id/showImageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="center"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_default_image_placeholder_48" />

        <TextView
            android:id="@+id/titleTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="16dp"
            android:fontFamily="@font/open_sans_semi_bold"
            android:textColor="@color/secondaryColor"
            android:textSize="16sp"
            android:maxLines="1"
            app:layout_constraintLeft_toRightOf="@id/showImageView"
            app:layout_constraintRight_toLeftOf="@id/rightArrowImageView"
            app:layout_constraintTop_toTopOf="@id/showImageView"
            app:layout_constraintBottom_toBottomOf="@id/showImageView"
            tools:text="1 - Pilot" />

        <ImageView
            android:id="@+id/rightArrowImageView"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@drawable/ic_baseline_keyboard_arrow_right_24"
            app:tint="@color/white"
            app:layout_constraintRight_toRightOf="parent"
            android:layout_marginRight="16dp"
            app:layout_constraintTop_toTopOf="@id/showImageView"
            app:layout_constraintBottom_toBottomOf="@id/showImageView"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>